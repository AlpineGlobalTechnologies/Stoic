<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Daily Stoic" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="robots" content="noindex">
    <meta charset="utf-8">
    <meta name="description" content="<%= content_for(:meta_description) || "Welcome to the Daily Stoic." %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/michael-scott.webp" type="image/png">
    <link rel="icon" href="/michael-scott.webpr" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/michael-scott.webp">
    <%= favicon_link_tag 'michael-scott.webp', type: 'image/png' %>

     <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <%= render "shared/header" %>

  <main>
    
      <div class="notification-block">
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
      </div>
      <%= yield %>
   
  </main>

  <%= render "shared/footer" %>
</html>
